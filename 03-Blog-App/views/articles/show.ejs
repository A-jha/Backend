<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <title><%= article.title %></title>
    <style>
      .bar {
        width: 100px;
        height: 5px;
        background: rgb(211, 174, 10);
        margin: 0 auto;
      }
      img {
        width: 100%;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="text-center mt-3"><%= article.title %></h1>
      <div class="bar"></div>
      <div class="text-center text-muted">
        <%= article.createdAt.toDateString() %>
      </div>
      <div class="mt-3">
        <a class="btn btn-outline-dark mx-2" href="/">All Articles</a>
        <a
          class="btn btn-info text-white"
          href="/articles/edit/<%= article.id  %>"
          >Edit</a
        >
      </div>
      <div class="mt-2"><b><%= article.description %></b></div>
      <div class="mt-2"><%- article.sanitizedHtml %></div>
    </div>
  </body>
</html>
